# 🏋️ Lista de Exercícios: Ponte para IA (Módulo 5)

## 🧪 Exercício: JSON de Configuração
Crie um dicionário chamado `config` com `modelo="gemini"` e `versao=1.5`. Converta-o para JSON usando `json.dumps()` e imprima.
